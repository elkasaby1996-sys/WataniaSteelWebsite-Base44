# Al Watania Steel Website

This repo contains the Base44-exported React sections and a Vite + React scaffold so the site can run locally.

## Getting Started

```bash
npm install
npm run dev
```

Vite will print a local development URL (usually `http://localhost:5173`).
